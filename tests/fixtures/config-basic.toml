# basic fixture
model = "gpt-5.6-sol"
model_provider = "custom"
web_search = "cached"

[model_providers.custom]
name = "Custom"
base_url = "https://provider.example.invalid/v1"
wire_api = "responses"
